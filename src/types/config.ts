export interface ThemeConfig {
    theme: string;
    position: string;
    primaryColor: string;
    secondaryColor: string;
    textColor: string;
    headerText: string;
    inputPlaceholder: string;
    width: string;
    height: string;
    borderRadius: string;
    launcher: boolean;
    showHeader: boolean;
}
